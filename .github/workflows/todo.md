- dev no longer uses docker
- no more mypy inside of ros
- markdown runs doclink
- blueprints check runs first: `dimos/robot/test_all_blueprints_generation.py::test_all_blueprints_is_current`
- run docker stuff on main
- maybe add `bin/ci`

- ci with arm

intermediate:
- why does bin/pytest-fast fail even on 100% success?